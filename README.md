# IDE
# 💻 Mini IDE — Thari Edition

**Mini IDE (Thari Edition)** is a lightweight Python-based code editor built using **Tkinter**.  
It allows you to **write**, **run**, and **save** Python scripts — all in a simple, elegant GUI with syntax highlighting and console output.

---

## ✨ Features

✅ **Open & Save Files** — load `.py` files or create new ones  
✅ **Run Code Instantly** — executes Python code inside the IDE  
✅ **Console Output** — displays program output and errors  
✅ **Syntax Highlighting** — automatic colorization using `idlelib`  
✅ **Auto Brace Closing** — automatically inserts matching braces and quotes  
✅ **Clear Console Button** — one-click cleanup for fresh runs  
✅ **Simple UI** — modern and easy-to-use interface  

---

## 🧠 Technologies Used

- **Python 3**
- **Tkinter** (for GUI)
- **idlelib** (`ColorDelegator`, `Percolator`) — for syntax highlighting
- **subprocess** — to run Python scripts safely

---

## 🧩 Project Structure

Mini-IDE-Thari/
├── main.py # Main application (Mini IDE)
├── README.md # Project documentation
└── temp_code.py # Temporary file generated during execution
